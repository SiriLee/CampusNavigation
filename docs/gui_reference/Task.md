## 任务要求：图形化界面

### 3.1 场景与任务

把项目从命令行升级为图形化界面。任意图形库 / Web 技术皆可（Qt、Dear ImGui、HTML+canvas、D3、PyQt + matplotlib、Graphviz 导出 SVG 等）。

### 3.2 必须展示的内容

- 节点位置（手动布局 或 自动布局均可）；
- 边连接，区分 `open` / `closed`；
- 最短路径在图上高亮；
- 关键节点 / 关键边在图上标识。

### 3.3 提交

- 截图（至少 3~5 张，覆盖上述必须展示项）；
- 简要技术说明（用什么库、怎么编译/运行）。

> 注意：图形化界面是**额外**做的，**不能替代必做的 CLI 评测接口**——必做的命令行入口仍需保留。

## GUI开发约束文档